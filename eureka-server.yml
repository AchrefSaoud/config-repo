spring:
  application:
    name: eureka-server
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
server:
  port: 8761
  
management:
  metrics:
    tags:
      application: ${spring.application.name}